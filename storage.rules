rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Demo rules for a small client-side project.
    // Tighten these before using in production.
    match /{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
